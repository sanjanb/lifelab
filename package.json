{
  "name": "lifelab",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A thinking surface for self-tracking - Vanilla JS analytical dashboard",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^7.2.4"
  }
}
